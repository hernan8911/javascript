// Acceder valores del producto 
/*const producto = {
    name: 'televisor 20 pulgadas ',
    precio : 300,
    aprobacion: false, 
    
mostrarInfo : function() {
    console.log( `el producto:  ${this.name} tiene un precio de:  ${this.precio}`)
}
}

const producto2 = {
    name: 'Tablet ',
    precio : 1000,
    aprobacion: false, 
    
mostrarInfo : function() {
    console.log( `el producto:  ${this.name} tiene un precio de:  ${this.precio}`)
}
}
producto.mostrarInfo();
producto2.mostrarInfo();
*/